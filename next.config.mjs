/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com', 'cdn.sanity.io', 'cdn.pixabay.com', 'cdn.statically.io'],
    },
};

export default nextConfig;
